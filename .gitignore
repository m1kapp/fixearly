node_modules/
*.log

# 엔진이 남기는 로컬 측정 이력 — 기계마다 값이 달라 공유하면 진행도가 거짓말을 한다.
.fixearly-history.json
.fixearly-sweep.json
.vercel

# fixearly 자기 실행 산출물 — 사이트와 무관하다.
# vercel.json 이 없어서 public/ 이 커밋되면 Vercel 이 그걸 출력 디렉터리로 잡고
# 루트 index.html 이 404 가 된다. 실제로 한 번 그렇게 내려갔다.
public/

# 사내 비공개 저장소 대시보드 — 생성 결과·저장소명·등급을 절대 공개 배포에 싣지 않는다.
.internal/

# 개인 Flow MCP 대시보드·상류 수정 연구 — 사내 메시지, 저장소명, 보안 경로를 포함한다.
.flow-insights-auth.json
.flow-insights-cache.json
flow-insights-*.html
tools/flow-insights-server.mjs
UPSTREAM-FIX-GAP*.md
